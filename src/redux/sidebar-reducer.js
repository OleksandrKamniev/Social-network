let initialState = {
    friends: [
        { friendName: 'John' },
        { friendName: 'Ben' },
        { friendName: 'Dany' },
    ]
}

const sidebarReducer = (state = initialState, action) => {


    return state;
}

export default sidebarReducer;